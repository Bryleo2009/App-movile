<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ejer1"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".FormProductActivity"
            android:exported="false" />
        <activity
            android:name=".ListClientActivity"
            android:exported="false" />
        <activity
            android:name=".ListProduct"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.example.app.DEVELOPER_NAME"
            android:value="Nombre del desarrollador" />
        <meta-data
            android:name="com.example.app.SUPPORT_EMAIL"
            android:value="soporte@appexample.com" />
        <meta-data
            android:name="com.example.app.APP_VERSION"
            android:value="1.0" />
        <meta-data
            android:name="api_key"
            android:value="your_api_key_here" />
        <meta-data
            android:name="company_name"
            android:value="Your Company Name" />
    </application>

</manifest>